Place optional overlay test assets (e.g., test-avatar.png) here. They will be served at /assets/* by the overlay server.

You can replace test-avatar.png with any image file and the test flows will use it if configured to /assets/test-avatar.png.